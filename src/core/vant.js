import Vue from 'vue'
import {
  Button,
  Cell,
  Form,
  Field,
  List,
  NavBar,
  Popup,
  PullRefresh,
  Tabbar,
  TabbarItem,
  Tag,
  Toast
} from 'vant'

Vue.use(Button)
.use(Cell)
.use(Form)
.use(Field)
.use(List)
.use(NavBar)
.use(Popup)
.use(PullRefresh)
.use(Tabbar)
.use(TabbarItem)
.use(Tag)
.use(Toast)
